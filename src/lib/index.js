/* eslint-disable no-unused-vars */
/* eslint-disable no-alert */
// aqui exportaras las funciones que necesites
export const myFunction = () => {
  // aqui tu codigo
};

// const borrar = divMuro.querySelector('#delete');
// borrar.addEventListener('click', () => {
//   firestore.collection('coment').doc('coment').delete().then(() => {
//     console.log('Document successfully deleted!');
//   })
//     .catch((error) => {
//       console.error('Error removing document: ', error);
//     });
// });

// export const eliminarPostAlClick = (idPost, idUserOfPost) => {
//   const currentUserData = firebase.auth().currentUser;
//   const uidOfCurrentUser = currentUserData().uid; // id del usuario logueado actual
//   if (uidOfCurrentUser === idUserOfPost) {
//     promiseOfdeleteFirebase('posts', idPost)
//       .then(() => {})
//       .catch(() => {
//         // console.error('Error removing document: ', error);
//       });
//   } else {
//     alert('No puedes eliminar un comentario que no sea tuyo Padawan');
//   }
// };
